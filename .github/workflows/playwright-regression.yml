name: Playwright Regression

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: playwright-regression-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  project-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run project checks
        run: npm run check

  regression:
    needs: project-checks
    name: Playwright (${{ matrix.label }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: chromium
            label: Chromium
            os: ubuntu-latest
            playwright_project_group: chromium
            playwright_workers: 6
            install_command: npx playwright install --with-deps chromium
            test_command: npm run check:browser:ci
          - id: firefox
            label: Firefox
            os: ubuntu-latest
            playwright_project_group: firefox
            playwright_workers: 1
            install_command: npx playwright install --with-deps firefox
            test_command: xvfb-run --auto-servernum npm run check:browser:ci
          - id: webkit
            label: Safari WebKit
            os: macos-latest
            playwright_project_group: webkit
            playwright_workers: 1
            install_command: npx playwright install webkit
            test_command: npm run check:browser:ci
    env:
      HAKOMACHI_PLAYWRIGHT_PROJECTS: ${{ matrix.playwright_project_group }}
      HAKOMACHI_PLAYWRIGHT_WORKERS: ${{ matrix.playwright_workers }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser
        run: ${{ matrix.install_command }}

      - name: Run Playwright regression suite
        run: ${{ matrix.test_command }}

      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-artifacts-${{ matrix.id }}
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
