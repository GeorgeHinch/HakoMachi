import * as bootstrap from './bootstrap.js';
import * as githubPersistence from './github-persistence.js';
import * as runtimeSurface from './runtime-surface.js';

export const appModules = Object.freeze({
  bootstrap,
  githubPersistence,
  runtimeSurface,
});
